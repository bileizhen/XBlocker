# 翻译 / Localization

XBlocker 使用 Android 字符串资源，参考 [SukiSU 的 LocaleHelper](https://github.com/SukiSU-Ultra/SukiSU-Ultra/blob/main/manager/app/src/main/java/com/sukisu/ultra/ui/util/LocaleHelper.kt) 和按语言组织的 `strings.xml`。

设置 → 语言支持跟随系统、English、简体中文、繁體中文。Android 13 及以上使用系统应用语言 API，也可从系统设置修改；Android 9–12 保存应用偏好并重建 Activity。未支持的语言回退到英文。

| 资源目录 | 用途 |
| --- | --- |
| `app/src/main/res/values/strings.xml` | 英文及默认回退 |
| `app/src/main/res/values-zh/strings.xml` | 中文默认回退 |
| `app/src/main/res/values-b+zh+Hans/strings.xml` | 简体中文 |
| `app/src/main/res/values-b+zh+Hant/strings.xml` | 繁体中文 |

## 新增或修改翻译

1. 在默认 `strings.xml` 添加有语义的资源名称，并更新相应语言文件。简体中文的两个目录应保持一致。
2. 保留 `%1$s` 等带序号的参数；允许调整参数顺序。需要格式化的字符串中的百分号写成 `%%`。数量文案使用不依赖单复数的形式；需要语法单复数时改用 Android `plurals`。
3. 新语言还需加入 `res/xml/locales_config.xml`、`AppLanguage.tags` 和设置中的语言列表。语言名称使用其原生写法。
4. Compose UI 使用 `LocalResources.current` 获取资源以观察配置变化；ViewModel 在显示消息时通过 `AppLanguage.context()` 获取资源，避免旧版 Android 使用过期语言。
5. 不翻译规则正文、用户名、存储键、包名或 URL。现有分类及历史记录中的标签由 `LocalizedText` 在显示时翻译，未知的云端分类原样显示。许可证原文和在线更新说明保留来源语言。

通知在宿主 X 进程中会读取模块资源；若应用隐藏导致模块资源不可访问，则回退到内置英文，避免通知发布失败。

## 验证

```powershell
.\gradlew.bat :core:test :app:testDebugUnitTest :app:assembleDebug :app:lintDebug
.\gradlew.bat :app:assembleDebugAndroidTest
adb install -r app/build/outputs/apk/debug/app-debug.apk
adb install -r app/build/outputs/apk/androidTest/debug/app-debug-androidTest.apk
adb shell am instrument -w -e class io.github.xblocker.LocalizationTest io.github.bileizhen.xblocker.test/androidx.test.runner.AndroidJUnitRunner
```

`LocalizationTest` 检查 Android 实际资源选择、未支持语言回退、格式参数和存储标签的显示转换。切换语言时编辑器保存的是稳定的 `custom` / `whitelist` 类型，因此未保存的文本不会因标题翻译而切换类型。

上面的真机命令保留调试应用安装；Gradle 的 `connectedDebugAndroidTest` 流程可能在结束时卸载被测应用，应仅用于专用测试设备。
